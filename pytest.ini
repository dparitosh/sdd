[pytest]
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
norecursedirs =
    legacy
addopts = 
    -v
    --strict-markers
    --cov=backend/src
    --cov-report=html
    --cov-report=term-missing
